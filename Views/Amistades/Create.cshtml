@model RedSocial.Models.Amistad

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Amistad</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EmailSender" class="control-label"></label>
                <select asp-for="EmailSender" class ="form-control" asp-items="ViewBag.EmailSender"></select>
            </div>
            <div class="form-group">
                <label asp-for="EmailReciver" class="control-label"></label>
                <select asp-for="EmailReciver" class ="form-control" asp-items="ViewBag.EmailReciver"></select>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Aceptada" /> @Html.DisplayNameFor(model => model.Aceptada)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
